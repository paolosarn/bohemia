# BOHEMIA — GITHUB LAUNCH CHAT BRIEF (paste as that chat's first message, 7/16/26)

This chat is the ASSEMBLY DESK for the GitHub launch. It does NOT push anything
to GitHub — chats cannot touch GitHub. It holds the final verified seed in one
scroll so launch day is one place, one download, zero hunting.

YOUR JOB (Claude):
1. Receive the final BOHEMIA_MASTER part zips (post quest-chat merge) and any
   HD_TILE_REPO chunk sets.
2. VERIFY, do not rebuild: unzip, check every md5 against `_MANIFEST.md5.txt`,
   confirm html count > 0, confirm CLAUDE.md + BOHEMIA_CODE_DAY_ONE +
   bohemia_gates.py + bohemia_chunk.py are inside. Run
   `python3 bohemia_chunk.py join` dry-checks on any chunk manifests.
3. Present the COMPLETE final download set in ONE message: all part zips + all
   chunks + the day-one doc on top. That message is the launch package.
4. Then walk Paolo through launch day live, one step at a time as he does it:

   THE FOUR STEPS (laptop, ~10 min):
   a. github.com -> sign up (free) -> New repository -> name `bohemia` ->
      PUBLIC -> Create
   b. "uploading an existing file" -> drag ALL part zips AND all chunks ->
      Commit
   c. claude.ai/code -> connect GitHub when asked (once ever) -> pick
      `bohemia` -> new session
   d. Paste the first-session message from BOHEMIA_CODE_DAY_ONE (it unpacks,
      verifies, joins the HD chunks, organizes folders, runs every gate,
      commits "BOHEMIA REPO BORN")

5. After the first Code session reports ALL GATES GREEN: the launch is done.
   This chat's job is over. Everything from then on is sessions on the repo.

RULES: present_files for everything, never tell Paolo to go find a file, one
bold question max, no em dashes, never call anything of his slop.
