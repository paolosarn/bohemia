# THE STREET TAKES ITS WORDS BACK
FACTIONS lane · THE FIVE MINUTES (rule 14), round four · 9/13/26

## THE ONE LINE
One blocked step put *"somebody steps into your way. they meant to."* on the
street, and forty blocks across the valley later it was still there, word for
word, with nobody anywhere near you. It was true for one step and a lie for the
rest of the day. It takes its words back now.

## THE MEASUREMENT
`[same lender]` is still **CLAIMED AND HELD** under 14b. The round went on the five
minutes, and the handoff named the street line as the next thing nobody had asked
about. *Glitches* is on his break list.

**Five writers share one line**, `#packline`:

| writer | whose | clears after itself |
|---|---|---|
| `walkSay` — the road and its encounters | another lane | **no** |
| `ctAgainstSay` — somebody steps into your way | **this lane** | **no** → now yes |
| `packButton` and its tap — the pack | another lane | on its own path |
| `trackSay` — whose footprints these are | this lane | yes, own words only |

Driven on the demo, on a phone: say it, then walk forty real blocks and read the
line off the page at every step. **It never changed once.**

## AND IT FROZE THE WHOLE LINE, NOT JUST ITS OWN SENTENCE
This is the part that makes it worse than a stale string. `trackSay` clears **only
its own words** — correctly, because the line is shared and wiping it
unconditionally would delete somebody else's sentence. So once this sentence sat
there, the track line could never clear it, and its own change-guard stopped it
ever speaking again.

**One writer that does not tidy up silences every writer that does.**

## THE FIX IS THE DISCIPLINE THAT WAS ALREADY IN THE FILE
No new hook and no new element: the call that already runs on movement clears it,
and only while the line still says exactly what this put there. It is cleared
**before** the track read runs, so the line is free when the tracks get their
chance at it — the other order leaves them silent behind a sentence that is
already a lie.

    said        "somebody steps into your way. they meant to."
    3 blocks on  ""   (was: unchanged after forty)

## THE SAME DEFECT, ONE WRITER OVER, AND IT IS NOT THIS LANE'S
`walkSay` writes the road and encounter lines and never clears either. Measured in
the same run:

    walkSay('feral_dog_pack')  ->  "they get up when you get close."
    eight blocks later         ->  "they get up when you get close."

Same shape, same consequence, same one-function fix. It belongs to the lane that
owns the road encounters, handed over with the reproduction rather than reached
into.

## GATES
`against_gate` extended by two claims, and the first is **driven, not grepped**:
open the demo, say it, walk, and read the line off the page. The second pins that
it clears **only its own words**, because a clear that wipes the line
unconditionally would fix this glitch by creating the one it exists to avoid.

## RULE 14, OBSERVED
No demo cut, no alpha touched, no build stamp. Fourth round running. The change is
in the city file, which both surfaces load by reference.

## [PENDING Paolo] — NOTHING NEW
