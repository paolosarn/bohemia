# THE DEMO HAS A FRONT DOOR NOW (8/26/26, SOUND lane taking an unclaimed SHARED row)

His ruling, 8/25, LOCKED:

> "BRO THE DEMO IS NOT LIVE ARE YOU SERIOUS THE DEMO WILL BE A STANDALONE LINK
> THAT ISNT THIS WORKSHOP LINK ARE YOU SERIOUS AND WE ARE NOT READY FOR THE DEMO
> YET! WHAT ARE WE STILL MISSING!"

The coordinator's measured gap list makes item A the headline and says of it in
capitals: **"THIS IS THE HEADLINE AND EVERYTHING ELSE IS SMALLER THAN IT."** Zero
standalone slices existed. Nothing published a player-only file.

It exists now: **`slices/BOHEMIA_DEMO.html`**, generated, gated, 24 claims green
on both surfaces.

## WHY THIS LANE TOOK IT

SOUND's own two assigned reds closed yesterday and its demo work is done: the day
sounds in 10 of its 11 beats and the 11th is the splash, silent on purpose. This
row is SHARED, unclaimed, and named as the thing blocking everything else. Main
had not moved since this lane's last push, so nobody was mid-flight on it.

## IT IS A CUT, AND THE MACHINE HOLDS THAT PROMISE

The law says the demo is **cut from the workshop, never a fork** -- one engine,
one canon, one set of modules, and what differs is only what is REACHABLE. A
hand-made demo file is a fork the moment the alpha moves, and it rots silently,
which is the exact class of thing this repo has spent a month finding.

So the demo is GENERATED by `tools/bohemia_cut_the_demo.js`, and
`demo_build_gate` **re-runs the cutter and fails unless the committed demo is
byte-identical to what it produces**. That check is the whole anti-fork
guarantee. Everything else is bookkeeping, and every mutation below trips it as
well as the specific claim it targets -- the two brace each other, so the demo
cannot quietly drift away from his bench.

## WHAT THE CUT ACTUALLY REMOVES

**1. Fifteen dev tabs.** The bar keeps exactly one, RUN, and it is never seen.
Measured before cutting, because removing markup other code queries is how you
ship a black rectangle: **only `.tab[data-p="run"]` is ever looked up by selector
anywhere in the alpha.** Every other tab is reached as `t.dataset.p` inside the
click handler, which cannot run for an element that no longer exists.

**2. The tab bar itself.** `#app` is a flex column of `#tabs` then
`#stage{flex:1}`, so hiding the bar hands the game the whole screen. The RUN tab
stays in the DOM because **the splash clicks it** -- the alpha's own entry
handler does `querySelector('.tab[data-p="run"]').click()`, and the comment above
it explains why it must be a real click: the city iframe is built lazily inside
that handler, which also sends the player, sends the cast, restores the city save
and pushes approved prefabs. A markup default would show an empty panel and skip
all five. **The demo boots down the game's own path and duplicates none of it.**

**3. The builder's drawer inside the city, which was not on the routed
checklist and is the one that would have hurt somebody.** The walked surface's
toolbar is 🎵 MUSIC / 💾 / 📱 PHONE / 🛠, and the 🛠 opens `#devtray`: REROLL,
UNDER, KEY, PEOPLE, SLIDE. **A stranger tapping REROLL regenerates the world
underneath their own session.** That is not a cosmetic leak, it is a destroyed
playthrough, and "a stranger opens it and the only thing there is the game" is
the law it breaks.

It is hidden **from the demo side, not by editing the city.**
`BOHEMIA_CITY_WORLD.html` belongs to another lane and ONE SYSTEM ONE SESSION
means this tool does not reach into it. The demo injects one stylesheet into the
frame it owns, same-origin, in a try/catch that degrades to exactly today's
behaviour if a browser refuses. The city file is untouched and **the workshop
keeps its drawer**, asserted.

**4. The fallback onto his wardrobe bench -- and this one was found by mutating
the gate, not by reading the file.** With the RUN tab removed, the demo did not
land on nothing. It landed on `p-char`, the CHARACTER workbench, because the
alpha's markup carries `class="panel on" id="p-char"` as its at-rest default. The
real demo never reaches that state (the splash's click moves `on` to `p-city`,
measured), but a build for strangers must not keep a fallback whose failure mode
is "you are now in the developer's wardrobe tool". The demo now has **no at-rest
panel at all**, so its worst case is a blank stage.

## WHAT IT DELIBERATELY DOES NOT DO

- **It does not delete the dev panels.** They are inert without a tab to activate
  them: every heavy iframe is created lazily in the click handler, and
  `#voteFrame` carries `data-src` rather than `src`, so nothing is fetched.
  Deleting markup that thirty gates read is a large risk for a claim the law does
  not make. The law is about what is REACHABLE.
- **It does not touch blocker F, and saying otherwise would be a lie.** 24
  seconds to first play is real and it is RUN's. Measured here: the alpha is
  4.11 MB, of which `COMBAT_B64` is 1.6 MB and **the demo needs the fight**.
  Cutting tabs saves 125 KB of `RIG_B64` at most.

## IT BOOTS AND IT PLAYS ARE DIFFERENT CLAIMS

A cut that landed on a pretty static frame would pass every reachability check
and be useless to a stranger. So the gate also asks the frame it lands on whether
it owns the day loop, and taps the one button a player reaches for first:

    DAY defined, DQ defined, DAY 1
    the wake card is up -- a stranger arrives on the FIRST MORNING
    the phone opens when tapped

## A CORRECTION TO THE LAW'S OWN WORDING, MEASURED

The law says the workshop has "all seventeen tabs". **The bar holds SIXTEEN:**
VOTE LOOK WORDS CUTSCENE DIRECT RUN CHARACTER CLOTHES ANIMATION RIG COMBAT MUSIC
MAP SLICE LIFE ART. CITY is the seventeenth SURFACE but has never been a tab --
the RUN tab routes to the city panel (Paolo 7/28, "can you put the city in the
run tab?"). Seventeen surfaces, sixteen tabs. The gate asserts what is actually
there and names the count, so a future reader is not left choosing between two
numbers.

## A NUMBER THE FLEET SHOULD KNOW

The published surface went **246 MB to 250 MB against a 260 MB cap.** The demo
costs 4 MB and leaves about 10 MB of headroom. Not a blocker today; it is the
kind of thing that becomes one silently.

## MUTATIONS (IF YOU ADD A CHECK, MUTATE IT)

    A  a dev tab sneaks back into the demo      -> ZERO DEV TABS red (tabs: run music)
    B  the builder's drawer becomes reachable   -> DRAWER UNREACHABLE red (VISIBLE)
    C  the tab bar is shown                     -> NO TAB BAR red (true / true)
    D  the cut damages his bench (a tab lost)   -> WORKSHOP UNTOUCHED red (15 tabs)
    E  the alpha changes shape under the cutter -> the cutter REFUSES, exit 2:
                                                   "there is no RUN tab to keep, so
                                                   the demo would have no way to
                                                   open the game"
    G  the phone button hidden in the demo      -> PLAYER'S BUTTONS red (hidden)
    H2 the demo cannot open the game at all     -> every boot AND play claim red,
                                                   which is how the p-char fallback
                                                   was discovered
    I  the bench fallback comes back            -> NOTHING IS OPEN AT REST red (p-char)

A, B, C, G and I each trip the anti-fork check as well as their own claim.

**A LIMIT, STATED.** Mutation H aimed to tear `DAY` out of the loaded city from
the demo side and **did not land** -- `DAY` is not a window property, so the
assignment was a no-op and the gate stayed green on a mutation that changed
nothing. It is recorded here rather than quietly dropped. The day-loop claim is
proved non-vacuous by H2 (no game frame at all makes it red), not by a targeted
teardown. If somebody finds a landing version, it belongs in this gate.

## WHERE THIS SITS IN HIS ORDER

The order is **BUILD -> DOOR -> ENDING -> INSTRUMENT -> INVITE**, and nobody is
invited until all four exist. This is BUILD. The other three are RUN's and are
untouched by this turn. **Nobody should be handed the demo link yet** -- he said
we are not ready and he is right; the ending in particular is half of what a
person keeps and nobody has designed it.

The demo's URL, for the record and not for sending to anybody:
`https://paolosarn.github.io/bohemia/slices/BOHEMIA_DEMO.html`

## AND THEN IT WENT STALE TWELVE MINUTES LATER, WHICH IS THE REAL LESSON

Rebasing onto twelve commits from other lanes, the anti-fork check fired on its
first real test:

    THE DEMO IS NOT A CUT OF THE CURRENT WORKSHOP: regenerating it changes
    9846 bytes

Nothing was wrong. Other lanes edited the alpha, which is their job, and the
demo is a function of the alpha. Re-cut, 25/0 on the merged tree.

**But that means every alpha edit by any lane makes the committed demo stale, and
"somebody remembers to regenerate it" is exactly the manual step that left
`slices/BOHEMIA_RUN_CURRENT.html` four days behind `engine/` with nothing
noticing.** Repeating a pattern this repo has already proved rots would be a
choice, not an accident.

So **the deploy cuts it.** `.github/workflows/pages.yml` runs the cutter before
assembling the site, and because that job runs under `set -e`, a cutter that
REFUSES (exit 2, when the alpha has changed shape under it) fails the build
loudly rather than publishing a half-demo to strangers. Production cannot serve a
stale demo even if every lane forgets.

The committed copy and the strict gate stay. A red there is not noise: it is the
signal "your alpha change altered the demo, look at it", and it costs one command
to clear.

    MUTATION J  the deploy stops cutting the demo -> DEPLOY CUTS IT FRESH red

**And J caught a bug in my own check first.** Its first version searched the
workflow for the cutter's path and passed on the COMMENT above the step
explaining why the step exists, so deleting the step left it green. It now
matches the `run:` line itself. Third time this week a check of mine has matched
a sentence instead of code, and the only reason it is not still wrong is that I
mutated it.
